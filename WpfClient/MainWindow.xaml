<Window x:Class="WpfClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Title="Service Viewer" Width="1400" Height="700">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" /> <!-- Log Panel -->
            <ColumnDefinition Width="1*" /> <!-- HTML Viewer Panel -->
        </Grid.ColumnDefinitions>

        <!-- Log Output Panel -->
        <RichTextBox x:Name="logRichTextBox" Grid.Column="0" 
                     IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>

        <!-- HTML Viewer Panel -->
        <WebBrowser x:Name="htmlViewer" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</Window>
